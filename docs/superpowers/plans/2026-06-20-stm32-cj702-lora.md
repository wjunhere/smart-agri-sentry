# STM32F103RCT6 + CJ-702 + LoRa 采集转发实现计划

> **For agentic workers:** REQUIRED SUB-_SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 STM32F103RCT6 上新建一个 STM32CubeMX HAL 工程，通过 USART2 接收 CJ-702 空气传感器数据，每 60 秒计算平均值，并通过 USART3 以自定义二进制帧格式发送给 LoRa 模块。

**Architecture:** 采用定时器驱动状态机，无 RTOS。USART2 RX 使用中断 + 环形缓冲区解析 CJ-702 原始帧；解析成功后直接累加进 aggregator；TIM2 每秒产生 tick，60 秒时触发 LoRa 帧打包与发送。

**Tech Stack:** STM32CubeMX HAL、gcc-arm-none-eabi（STM32CubeCLT）、ST-Link（用户已切换）、CJ-702、E22-400T30S LoRa。

---

## 文件结构

```
test/stm32_cj702_lora_hal/
├── Core/
│   ├── Inc/
│   │   ├── main.h
│   │   ├── cj702.h
│   │   ├── aggregator.h
│   │   ├── lora_frame.h
│   │   ├── lora_tx.h
│   │   ├── app_fsm.h
│   │   └── status_led.h
│   └── Src/
│       ├── main.c
│       ├── cj702.c
│       ├── aggregator.c
│       ├── lora_frame.c
│       ├── lora_tx.c
│       ├── app_fsm.c
│       ├── status_led.c
│       ├── stm32f1xx_it.c
│       ├── stm32f1xx_hal_msp.c
│       └── system_stm32f1xx.c
├── Tests/
│   └── host/
│       ├── test_cj702.c
│       ├── test_aggregator.c
│       ├── test_lora_frame.c
│       ├── test_main.c
│       └── Makefile
├── Drivers/              # CubeMX 生成
├── Makefile              # CubeMX 生成
├── startup_stm32f103rctx.s
└── stm32_cj702_lora_hal.ioc
```

---

## Task 1: STM32CubeMX 工程初始化

**Files:**
- Create: `test/stm32_cj702_lora_hal/stm32_cj702_lora_hal.ioc`
- Create: `test/stm32_cj702_lora_hal/Makefile`（由 CubeMX 生成）
- Create: `test/stm32_cj702_lora_hal/Core/Src/main.c`（由 CubeMX 生成，后续修改）

- [ ] **Step 1: 创建 CubeMX 工程**

打开 STM32CubeMX：
```bash
"/e/stm32/STM32CubeMX"
```

操作步骤：
1. File → New Project → 选择 MCU `STM32F103RCT6`。
2. 工程名：`stm32_cj702_lora_hal`。
3. 保存路径：`E:\smart_agri_sentry\test\stm32_cj702_lora_hal`。
4. Toolchain / IDE：选择 `Makefile`。

- [ ] **Step 2: 配置时钟**

RCC 配置：
- High Speed Clock (HSE)：Crystal/Ceramic Resonator（8MHz）。
- 时钟树：HSE → PLL ×9 → SYSCLK = 72MHz，APB1 = 36MHz，APB2 = 72MHz。

- [ ] **Step 3: 配置外设**

| 外设 | 参数 | 引脚 |
|---|---|---|
| USART2 | Mode: Asynchronous，Baud Rate: 9600，8/N/1 | PA2(TX), PA3(RX) |
| USART3 | Mode: Asynchronous，Baud Rate: 9600，8/N/1 | PB10(TX), PB11(RX) |
| TIM2 | Internal Clock，Counter Period = 9999，Prescaler = 7199（1Hz = 1s） | 无 |
| GPIO_Output | PC0, PC1, PC2（RGB LED，低电平点亮） | PC0/PC1/PC2 |

NVIC：开启 USART2 global interrupt，TIM2 global interrupt。SysTick 保持默认。

- [ ] **Step 4: 生成代码**

Project Manager → Code Generator：勾选 `Generate peripheral initialization as a pair of '.c/.h' files per peripheral`。

点击 `GENERATE CODE`。

- [ ] **Step 5: 验证生成结果**

```bash
ls test/stm32_cj702_lora_hal/Makefile
ls test/stm32_cj702_lora_hal/Core/Src/main.c
```

Expected: 两个文件均存在。

- [ ] **Step 6: 提交**

```bash
git add test/stm32_cj702_lora_hal/
git commit -m "chore: create stm32_cj702_lora_hal CubeMX base project"
```

---

## Task 2: 实现 CJ-702 解析器

**Files:**
- Create: `test/stm32_cj702_lora_hal/Core/Inc/cj702.h`
- Create: `test/stm32_cj702_lora_hal/Core/Src/cj702.c`
- Create: `test/stm32_cj702_lora_hal/Tests/host/test_cj702.c`

- [ ] **Step 1: 编写头文件 `cj702.h`**

```c
#ifndef __CJ702_H
#define __CJ702_H

#include <stdint.h>
#include <stdbool.h>

#define CJ702_FRAME_LEN 17
#define CJ702_HEADER_0  0x3C
#define CJ702_HEADER_1  0x02

typedef struct {
    uint16_t co2;       // ppm
    uint16_t hcho;      // raw
    uint16_t tvoc;      // ppb
    uint16_t pm25;      // ug/m3
    uint16_t pm10;      // ug/m3
    int16_t  temp;      // 0.01 C
    uint16_t humidity;  // 0.01 %RH
} cj702_data_t;

bool cj702_parse(const uint8_t *frame, cj702_data_t *out);
uint8_t cj702_checksum(const uint8_t *frame);

#endif
```

- [ ] **Step 2: 编写实现 `cj702.c`**

```c
#include "cj702.h"

uint8_t cj702_checksum(const uint8_t *frame)
{
    uint16_t sum = 0;
    for (int i = 0; i < CJ702_FRAME_LEN - 1; i++) {
        sum += frame[i];
    }
    return (uint8_t)(sum & 0xFF);
}

bool cj702_parse(const uint8_t *frame, cj702_data_t *out)
{
    if (frame == NULL || out == NULL) {
        return false;
    }
    if (frame[0] != CJ702_HEADER_0 || frame[1] != CJ702_HEADER_1) {
        return false;
    }
    if (cj702_checksum(frame) != frame[CJ702_FRAME_LEN - 1]) {
        return false;
    }

    out->co2      = ((uint16_t)frame[2] << 8) | frame[3];
    out->hcho     = ((uint16_t)frame[4] << 8) | frame[5];
    out->tvoc     = ((uint16_t)frame[6] << 8) | frame[7];
    out->pm25     = ((uint16_t)frame[8] << 8) | frame[9];
    out->pm10     = ((uint16_t)frame[10] << 8) | frame[11];

    uint8_t temp_int = frame[12];
    int sign = (temp_int & 0x80) ? -1 : 1;
    int mag  = temp_int & 0x7F;
    out->temp = (int16_t)(sign * (mag * 100 + frame[13]));

    out->humidity = ((uint16_t)frame[14] * 100) + frame[15];

    return true;
}
```

- [ ] **Step 3: 编写主机测试 `Tests/host/test_cj702.c`**

```c
#include <stdio.h>
#include <string.h>
#include "cj702.h"

static int tests_run = 0;
static int tests_passed = 0;

#define ASSERT_EQ(got, expected) do { \
    tests_run++; \
    if ((got) == (expected)) { tests_passed++; } \
    else { printf("FAIL: %s:%d got %d expected %d\n", __FILE__, __LINE__, (int)(got), (int)(expected)); } \
} while(0)

static void test_parse_valid_positive_temp(void)
{
    // co2=0x012F=303, hcho=0x0014=20, tvoc=0x0078=120, pm25=0x0023=35, pm10=0x0032=50
    // temp=+27.00 => B13=0x1B, B14=0x00; humidity=60.00 => B15=0x3C, B16=0x00
    uint8_t frame[CJ702_FRAME_LEN] = {
        0x3C, 0x02,
        0x01, 0x2F,
        0x00, 0x14,
        0x00, 0x78,
        0x00, 0x23,
        0x00, 0x32,
        0x1B, 0x00,
        0x3C, 0x00,
        0x00
    };
    uint16_t sum = 0;
    for (int i = 0; i < CJ702_FRAME_LEN - 1; i++) sum += frame[i];
    frame[CJ702_FRAME_LEN - 1] = (uint8_t)(sum & 0xFF);

    cj702_data_t d;
    ASSERT_EQ(cj702_parse(frame, &d), true);
    ASSERT_EQ(d.co2, 303);
    ASSERT_EQ(d.hcho, 20);
    ASSERT_EQ(d.tvoc, 120);
    ASSERT_EQ(d.pm25, 35);
    ASSERT_EQ(d.pm10, 50);
    ASSERT_EQ(d.temp, 2700);
    ASSERT_EQ(d.humidity, 6000);
}

static void test_parse_valid_negative_temp(void)
{
    uint8_t frame[CJ702_FRAME_LEN] = {
        0x3C, 0x02,
        0x00, 0x00,
        0x00, 0x00,
        0x00, 0x00,
        0x00, 0x00,
        0x00, 0x00,
        0x9B, 0x00,  // -27
        0x00, 0x00,
        0x00
    };
    uint16_t sum = 0;
    for (int i = 0; i < CJ702_FRAME_LEN - 1; i++) sum += frame[i];
    frame[CJ702_FRAME_LEN - 1] = (uint8_t)(sum & 0xFF);

    cj702_data_t d;
    ASSERT_EQ(cj702_parse(frame, &d), true);
    ASSERT_EQ(d.temp, -2700);
}

static void test_parse_bad_checksum(void)
{
    uint8_t frame[CJ702_FRAME_LEN] = {0x3C, 0x02};
    memset(frame + 2, 0, CJ702_FRAME_LEN - 3);
    frame[CJ702_FRAME_LEN - 1] = 0xFF;
    cj702_data_t d;
    ASSERT_EQ(cj702_parse(frame, &d), false);
}

int test_cj702(void)
{
    test_parse_valid_positive_temp();
    test_parse_valid_negative_temp();
    test_parse_bad_checksum();
    printf("cj702: %d/%d passed\n", tests_passed, tests_run);
    return tests_run == tests_passed ? 0 : 1;
}
```

- [ ] **Step 4: 编写 `Tests/host/test_main.c`**

```c
#include <stdio.h>

extern int test_cj702(void);
extern int test_aggregator(void);
extern int test_lora_frame(void);

int main(void)
{
    int rc = 0;
    rc |= test_cj702();
    rc |= test_aggregator();
    rc |= test_lora_frame();
    return rc;
}
```

- [ ] **Step 5: 创建占位测试文件（后续任务填充）**

`Tests/host/test_aggregator.c`：
```c
#include <stdio.h>
int test_aggregator(void) { return 0; }
```

`Tests/host/test_lora_frame.c`：
```c
#include <stdio.h>
int test_lora_frame(void) { return 0; }
```

- [ ] **Step 6: 编写 `Tests/host/Makefile`**

```makefile
CC = gcc
CFLAGS = -Wall -Wextra -std=c99 -I../../Core/Inc
SRC = test_main.c test_cj702.c test_aggregator.c test_lora_frame.c \
      ../../Core/Src/cj702.c ../../Core/Src/aggregator.c ../../Core/Src/lora_frame.c

.PHONY: all clean

all: host_tests
	./host_tests

host_tests: $(SRC)
	$(CC) $(CFLAGS) -o $@ $(SRC)

clean:
	rm -f host_tests
```

- [ ] **Step 7: 运行测试（cj702 真实测试通过，aggregator/lora_frame 为占位）**

```bash
cd test/stm32_cj702_lora_hal/Tests/host
make
```

Expected: 编译通过，`cj702` 测试通过；`aggregator` 和 `lora_frame` 占位测试返回 0，后续任务替换为真实实现。

- [ ] **Step 8: 提交**

```bash
git add test/stm32_cj702_lora_hal/Core/Inc/cj702.h test/stm32_cj702_lora_hal/Core/Src/cj702.c
git add test/stm32_cj702_lora_hal/Tests/host/test_cj702.c
git add test/stm32_cj702_lora_hal/Tests/host/test_main.c
git add test/stm32_cj702_lora_hal/Tests/host/test_aggregator.c
git add test/stm32_cj702_lora_hal/Tests/host/test_lora_frame.c
git add test/stm32_cj702_lora_hal/Tests/host/Makefile
git commit -m "feat(cj702): add parser and host unit tests"
```

---

## Task 3: 实现 Aggregator

**Files:**
- Create: `test/stm32_cj702_lora_hal/Core/Inc/aggregator.h`
- Create: `test/stm32_cj702_lora_hal/Core/Src/aggregator.c`
- Create: `test/stm32_cj702_lora_hal/Tests/host/test_aggregator.c`

- [ ] **Step 1: 编写头文件 `aggregator.h`**

```c
#ifndef __AGGREGATOR_H
#define __AGGREGATOR_H

#include <stdint.h>
#include <stdbool.h>
#include "cj702.h"

#define AGGREGATOR_MIN_SAMPLES 10

typedef struct {
    uint32_t co2_sum;
    uint32_t hcho_sum;
    uint32_t tvoc_sum;
    uint32_t pm25_sum;
    uint32_t pm10_sum;
    int32_t  temp_sum;
    uint32_t humidity_sum;
    uint16_t count;
} aggregator_t;

void aggregator_init(aggregator_t *agg);
void aggregator_add(aggregator_t *agg, const cj702_data_t *sample);
bool aggregator_get_average(const aggregator_t *agg, cj702_data_t *out);

#endif
```

- [ ] **Step 2: 编写实现 `aggregator.c`**

```c
#include "aggregator.h"

void aggregator_init(aggregator_t *agg)
{
    agg->co2_sum = 0;
    agg->hcho_sum = 0;
    agg->tvoc_sum = 0;
    agg->pm25_sum = 0;
    agg->pm10_sum = 0;
    agg->temp_sum = 0;
    agg->humidity_sum = 0;
    agg->count = 0;
}

void aggregator_add(aggregator_t *agg, const cj702_data_t *sample)
{
    agg->co2_sum      += sample->co2;
    agg->hcho_sum     += sample->hcho;
    agg->tvoc_sum     += sample->tvoc;
    agg->pm25_sum     += sample->pm25;
    agg->pm10_sum     += sample->pm10;
    agg->temp_sum     += sample->temp;
    agg->humidity_sum += sample->humidity;
    agg->count++;
}

bool aggregator_get_average(const aggregator_t *agg, cj702_data_t *out)
{
    if (agg == NULL || out == NULL || agg->count < AGGREGATOR_MIN_SAMPLES) {
        return false;
    }
    out->co2      = (uint16_t)(agg->co2_sum / agg->count);
    out->hcho     = (uint16_t)(agg->hcho_sum / agg->count);
    out->tvoc     = (uint16_t)(agg->tvoc_sum / agg->count);
    out->pm25     = (uint16_t)(agg->pm25_sum / agg->count);
    out->pm10     = (uint16_t)(agg->pm10_sum / agg->count);
    out->temp     = (int16_t)(agg->temp_sum / agg->count);
    out->humidity = (uint16_t)(agg->humidity_sum / agg->count);
    return true;
}
```

- [ ] **Step 3: 编写测试 `Tests/host/test_aggregator.c`**

```c
#include <stdio.h>
#include "aggregator.h"

static int tests_run = 0;
static int tests_passed = 0;

#define ASSERT_EQ(got, expected) do { \
    tests_run++; \
    if ((got) == (expected)) { tests_passed++; } \
    else { printf("FAIL: %s:%d got %d expected %d\n", __FILE__, __LINE__, (int)(got), (int)(expected)); } \
} while(0)

static void test_average(void)
{
    aggregator_t agg;
    aggregator_init(&agg);
    cj702_data_t s = {100, 10, 50, 20, 30, 2500, 5000};
    for (int i = 0; i < 30; i++) {
        aggregator_add(&agg, &s);
    }
    cj702_data_t out;
    ASSERT_EQ(aggregator_get_average(&agg, &out), true);
    ASSERT_EQ(out.co2, 100);
    ASSERT_EQ(out.temp, 2500);
    ASSERT_EQ(out.humidity, 5000);
}

static void test_too_few_samples(void)
{
    aggregator_t agg;
    aggregator_init(&agg);
    cj702_data_t s = {1, 2, 3, 4, 5, 6, 7};
    for (int i = 0; i < 5; i++) {
        aggregator_add(&agg, &s);
    }
    cj702_data_t out;
    ASSERT_EQ(aggregator_get_average(&agg, &out), false);
}

int test_aggregator(void)
{
    test_average();
    test_too_few_samples();
    printf("aggregator: %d/%d passed\n", tests_passed, tests_run);
    return tests_run == tests_passed ? 0 : 1;
}
```

- [ ] **Step 4: 运行测试**

```bash
cd test/stm32_cj702_lora_hal/Tests/host
make
```

Expected: cj702 和 aggregator 测试通过。

- [ ] **Step 5: 提交**

```bash
git add test/stm32_cj702_lora_hal/Core/Inc/aggregator.h test/stm32_cj702_lora_hal/Core/Src/aggregator.c
git add test/stm32_cj702_lora_hal/Tests/host/test_aggregator.c
git commit -m "feat(aggregator): add 60s averaging logic and tests"
```

---

## Task 4: 实现 LoRa 帧打包

**Files:**
- Create: `test/stm32_cj702_lora_hal/Core/Inc/lora_frame.h`
- Create: `test/stm32_cj702_lora_hal/Core/Src/lora_frame.c`
- Create: `test/stm32_cj702_lora_hal/Tests/host/test_lora_frame.c`

- [ ] **Step 1: 编写头文件 `lora_frame.h`**

```c
#ifndef __LORA_FRAME_H
#define __LORA_FRAME_H

#include <stdint.h>
#include "cj702.h"

#define LORA_FRAME_HEADER_0 0xAA
#define LORA_FRAME_HEADER_1 0x55
#define LORA_MSG_DATA       0x01
#define LORA_MSG_ERROR      0xFF
#define LORA_ERROR_TIMEOUT  0x01
#define LORA_ERROR_INCOMPLETE 0x02

typedef enum {
    LORA_OK = 0,
    LORA_ERR_NULL = -1,
    LORA_ERR_NO_SPACE = -2,
} lora_frame_status_t;

uint8_t crc8_maxim(const uint8_t *data, uint16_t len);
int lora_frame_pack_data(uint8_t device_id, const cj702_data_t *avg, uint8_t *buf, uint16_t buf_size);
int lora_frame_pack_error(uint8_t device_id, uint8_t error_code, uint8_t *buf, uint16_t buf_size);

#endif
```

- [ ] **Step 2: 编写实现 `lora_frame.c`**

```c
#include "lora_frame.h"
#include <string.h>

#define SENSOR_PAYLOAD_LEN 14
#define ERROR_PAYLOAD_LEN  1
#define FRAME_OVERHEAD     5  // header(2) + device_id(1) + msg_type(1) + len(1)

uint8_t crc8_maxim(const uint8_t *data, uint16_t len)
{
    uint8_t crc = 0x00;
    for (uint16_t i = 0; i < len; i++) {
        crc ^= data[i];
        for (uint8_t j = 0; j < 8; j++) {
            if (crc & 0x80) {
                crc = (crc << 1) ^ 0x31;
            } else {
                crc <<= 1;
            }
        }
    }
    return crc;
}

static int pack_frame(uint8_t device_id, uint8_t msg_type,
                      const uint8_t *payload, uint8_t payload_len,
                      uint8_t *buf, uint16_t buf_size)
{
    if (buf == NULL || payload == NULL) {
        return LORA_ERR_NULL;
    }
    if (buf_size < FRAME_OVERHEAD + payload_len + 1) {
        return LORA_ERR_NO_SPACE;
    }
    buf[0] = LORA_FRAME_HEADER_0;
    buf[1] = LORA_FRAME_HEADER_1;
    buf[2] = device_id;
    buf[3] = msg_type;
    buf[4] = payload_len;
    memcpy(&buf[5], payload, payload_len);
    buf[5 + payload_len] = crc8_maxim(buf, 5 + payload_len);
    return FRAME_OVERHEAD + payload_len + 1;
}

int lora_frame_pack_data(uint8_t device_id, const cj702_data_t *avg, uint8_t *buf, uint16_t buf_size)
{
    if (avg == NULL) {
        return LORA_ERR_NULL;
    }
    uint8_t payload[SENSOR_PAYLOAD_LEN];
    payload[0]  = (uint8_t)(avg->co2 >> 8);
    payload[1]  = (uint8_t)(avg->co2);
    payload[2]  = (uint8_t)(avg->hcho >> 8);
    payload[3]  = (uint8_t)(avg->hcho);
    payload[4]  = (uint8_t)(avg->tvoc >> 8);
    payload[5]  = (uint8_t)(avg->tvoc);
    payload[6]  = (uint8_t)(avg->pm25 >> 8);
    payload[7]  = (uint8_t)(avg->pm25);
    payload[8]  = (uint8_t)(avg->pm10 >> 8);
    payload[9]  = (uint8_t)(avg->pm10);
    payload[10] = (uint8_t)(avg->temp >> 8);
    payload[11] = (uint8_t)(avg->temp);
    payload[12] = (uint8_t)(avg->humidity >> 8);
    payload[13] = (uint8_t)(avg->humidity);
    return pack_frame(device_id, LORA_MSG_DATA, payload, SENSOR_PAYLOAD_LEN, buf, buf_size);
}

int lora_frame_pack_error(uint8_t device_id, uint8_t error_code, uint8_t *buf, uint16_t buf_size)
{
    return pack_frame(device_id, LORA_MSG_ERROR, &error_code, ERROR_PAYLOAD_LEN, buf, buf_size);
}
```

- [ ] **Step 3: 编写测试 `Tests/host/test_lora_frame.c`**

```c
#include <stdio.h>
#include <string.h>
#include "lora_frame.h"

static int tests_run = 0;
static int tests_passed = 0;

#define ASSERT_EQ(got, expected) do { \
    tests_run++; \
    if ((got) == (expected)) { tests_passed++; } \
    else { printf("FAIL: %s:%d got %d expected %d\n", __FILE__, __LINE__, (int)(got), (int)(expected)); } \
} while(0)

static void test_pack_data(void)
{
    cj702_data_t avg = {303, 20, 120, 35, 50, 2500, 6000};
    uint8_t buf[32];
    int len = lora_frame_pack_data(0x01, &avg, buf, sizeof(buf));
    ASSERT_EQ(len, 20);
    ASSERT_EQ(buf[0], 0xAA);
    ASSERT_EQ(buf[1], 0x55);
    ASSERT_EQ(buf[2], 0x01);
    ASSERT_EQ(buf[3], LORA_MSG_DATA);
    ASSERT_EQ(buf[4], 14);
}

static void test_pack_error(void)
{
    uint8_t buf[32];
    int len = lora_frame_pack_error(0x01, LORA_ERROR_TIMEOUT, buf, sizeof(buf));
    ASSERT_EQ(len, 7);
    ASSERT_EQ(buf[3], LORA_MSG_ERROR);
    ASSERT_EQ(buf[4], 1);
    ASSERT_EQ(buf[5], LORA_ERROR_TIMEOUT);
}

int test_lora_frame(void)
{
    test_pack_data();
    test_pack_error();
    printf("lora_frame: %d/%d passed\n", tests_passed, tests_run);
    return tests_run == tests_passed ? 0 : 1;
}
```

- [ ] **Step 4: 运行全部主机测试**

```bash
cd test/stm32_cj702_lora_hal/Tests/host
make
```

Expected: 全部测试通过。

- [ ] **Step 5: 提交**

```bash
git add test/stm32_cj702_lora_hal/Core/Inc/lora_frame.h test/stm32_cj702_lora_hal/Core/Src/lora_frame.c
git add test/stm32_cj702_lora_hal/Tests/host/test_lora_frame.c
git commit -m "feat(lora_frame): add custom binary frame packing and tests"
```

---

## Task 5: 实现 LoRa 发送与状态灯

**Files:**
- Create: `test/stm32_cj702_lora_hal/Core/Inc/lora_tx.h`
- Create: `test/stm32_cj702_lora_hal/Core/Src/lora_tx.c`
- Create: `test/stm32_cj702_lora_hal/Core/Inc/status_led.h`
- Create: `test/stm32_cj702_lora_hal/Core/Src/status_led.c`

- [ ] **Step 1: 编写 `lora_tx.h`**

```c
#ifndef __LORA_TX_H
#define __LORA_TX_H

#include <stdint.h>
#include "usart.h"

int lora_tx_send(const uint8_t *data, uint16_t len, uint32_t timeout_ms);

#endif
```

- [ ] **Step 2: 编写 `lora_tx.c`**

```c
#include "lora_tx.h"

extern UART_HandleTypeDef huart3;

int lora_tx_send(const uint8_t *data, uint16_t len, uint32_t timeout_ms)
{
    if (data == NULL || len == 0) {
        return -1;
    }
    HAL_StatusTypeDef status = HAL_UART_Transmit(&huart3, data, len, timeout_ms);
    return (status == HAL_OK) ? 0 : -1;
}
```

- [ ] **Step 3: 编写 `status_led.h`**

```c
#ifndef __STATUS_LED_H
#define __STATUS_LED_H

#include <stdint.h>

typedef enum {
    LED_STATE_NORMAL = 0,
    LED_STATE_TX,
    LED_STATE_ERROR,
} led_state_t;

void status_led_init(void);
void status_led_set(led_state_t state);

#endif
```

- [ ] **Step 4: 编写 `status_led.c`**

```c
#include "status_led.h"
#include "main.h"

void status_led_init(void)
{
    HAL_GPIO_WritePin(GPIOC, GPIO_PIN_0, GPIO_PIN_SET);  // R off
    HAL_GPIO_WritePin(GPIOC, GPIO_PIN_1, GPIO_PIN_SET);  // G off
    HAL_GPIO_WritePin(GPIOC, GPIO_PIN_2, GPIO_PIN_SET);  // B off
}

void status_led_set(led_state_t state)
{
    HAL_GPIO_WritePin(GPIOC, GPIO_PIN_0, GPIO_PIN_SET);
    HAL_GPIO_WritePin(GPIOC, GPIO_PIN_1, GPIO_PIN_SET);
    HAL_GPIO_WritePin(GPIOC, GPIO_PIN_2, GPIO_PIN_SET);

    switch (state) {
        case LED_STATE_NORMAL:
            HAL_GPIO_WritePin(GPIOC, GPIO_PIN_1, GPIO_PIN_RESET);  // Green
            break;
        case LED_STATE_TX:
            HAL_GPIO_WritePin(GPIOC, GPIO_PIN_2, GPIO_PIN_RESET);  // Blue
            break;
        case LED_STATE_ERROR:
            HAL_GPIO_WritePin(GPIOC, GPIO_PIN_0, GPIO_PIN_RESET);  // Red
            break;
    }
}
```

- [ ] **Step 5: 提交**

```bash
git add test/stm32_cj702_lora_hal/Core/Inc/lora_tx.h test/stm32_cj702_lora_hal/Core/Src/lora_tx.c
git add test/stm32_cj702_lora_hal/Core/Inc/status_led.h test/stm32_cj702_lora_hal/Core/Src/status_led.c
git commit -m "feat(lora_tx, status_led): add LoRa TX wrapper and RGB LED state indication"
```

---

## Task 6: 实现应用状态机与主循环

**Files:**
- Create: `test/stm32_cj702_lora_hal/Core/Inc/app_fsm.h`
- Create: `test/stm32_cj702_lora_hal/Core/Src/app_fsm.c`
- Modify: `test/stm32_cj702_lora_hal/Core/Src/main.c`
- Modify: `test/stm32_cj702_lora_hal/Core/Src/stm32f1xx_it.c`

- [ ] **Step 1: 编写 `app_fsm.h`**

```c
#ifndef __APP_FSM_H
#define __APP_FSM_H

#include <stdint.h>
#include <stdbool.h>

#define FSM_INTERVAL_SEC 60

typedef enum {
    FSM_ACCUMULATE = 0,
    FSM_TX,
    FSM_WAIT,
} fsm_state_t;

typedef struct {
    fsm_state_t state;
    uint8_t seconds;
    uint8_t no_valid_frame_seconds;
    bool one_second_elapsed;
} app_fsm_t;

void app_fsm_init(app_fsm_t *fsm);
void app_fsm_on_tick(app_fsm_t *fsm);
void app_fsm_run(app_fsm_t *fsm);

#endif
```

- [ ] **Step 2: 编写 `app_fsm.c`**

```c
#include "app_fsm.h"
#include "aggregator.h"
#include "lora_frame.h"
#include "lora_tx.h"
#include "status_led.h"
#include "cj702.h"
#include <stdint.h>
#include <string.h>

extern aggregator_t g_aggregator;
extern volatile uint8_t g_new_sample_ready;
extern cj702_data_t g_last_sample;

static uint8_t g_tx_buf[32];

void app_fsm_init(app_fsm_t *fsm)
{
    fsm->state = FSM_ACCUMULATE;
    fsm->seconds = 0;
    fsm->no_valid_frame_seconds = 0;
    fsm->one_second_elapsed = 0;
}

void app_fsm_on_tick(app_fsm_t *fsm)
{
    fsm->one_second_elapsed = 1;
}

void app_fsm_run(app_fsm_t *fsm)
{
    if (!fsm->one_second_elapsed) {
        return;
    }
    fsm->one_second_elapsed = 0;

    if (g_new_sample_ready) {
        g_new_sample_ready = 0;
        fsm->no_valid_frame_seconds = 0;
        aggregator_add(&g_aggregator, &g_last_sample);
    } else {
        fsm->no_valid_frame_seconds++;
    }

    fsm->seconds++;
    if (fsm->seconds < FSM_INTERVAL_SEC) {
        return;
    }
    fsm->seconds = 0;

    int len = -1;
    if (fsm->no_valid_frame_seconds >= FSM_INTERVAL_SEC) {
        status_led_set(LED_STATE_ERROR);
        len = lora_frame_pack_error(0x01, LORA_ERROR_TIMEOUT, g_tx_buf, sizeof(g_tx_buf));
    } else {
        cj702_data_t avg;
        if (aggregator_get_average(&g_aggregator, &avg)) {
            status_led_set(LED_STATE_TX);
            len = lora_frame_pack_data(0x01, &avg, g_tx_buf, sizeof(g_tx_buf));
        } else {
            status_led_set(LED_STATE_ERROR);
            len = lora_frame_pack_error(0x01, LORA_ERROR_INCOMPLETE, g_tx_buf, sizeof(g_tx_buf));
        }
    }

    if (len > 0) {
        lora_tx_send(g_tx_buf, (uint16_t)len, 1000);
    }

    aggregator_init(&g_aggregator);
    status_led_set(LED_STATE_NORMAL);
}
```

- [ ] **Step 3: 修改 `Core/Src/main.c`**

在 `main.c` 顶部 USER CODE include 区域添加：

```c
/* USER CODE BEGIN Includes */
#include "app_fsm.h"
#include "status_led.h"

extern app_fsm_t g_app_fsm;
/* USER CODE END Includes */
```

在 `MX_USART2_UART_Init()` 之后启动中断接收：

```c
/* USER CODE BEGIN 2 */
HAL_UART_Receive_IT(&huart2, &s_rx_tmp, 1);
status_led_init();
status_led_set(LED_STATE_NORMAL);
app_fsm_init(&g_app_fsm);
/* USER CODE END 2 */
```

在主循环中调用 FSM：

```c
/* USER CODE BEGIN WHILE */
while (1)
{
    app_fsm_run(&g_app_fsm);
    /* USER CODE END WHILE */
    /* USER CODE BEGIN 3 */
}
/* USER CODE END 3 */
```

在 `MX_TIM2_Init()` 后启动定时器中断：

```c
HAL_TIM_Base_Start_IT(&htim2);
```

- [ ] **Step 4: 修改 `Core/Src/stm32f1xx_it.c` 或 `usart.c` 的回调**

在 `Core/Src/usart.c` 的 `HAL_UART_RxCpltCallback` 中处理 USART2 数据（保留 CubeMX 生成部分，在 USER CODE 区间添加）：

```c
/* USER CODE BEGIN 1 */
#include "cj702.h"
#include "app_fsm.h"

#define CJ702_FRAME_LEN 17
static uint8_t s_cj702_raw[CJ702_FRAME_LEN];
static uint8_t s_cj702_idx = 0;
static uint8_t s_rx_tmp;

aggregator_t g_aggregator;
volatile uint8_t g_new_sample_ready = 0;
cj702_data_t g_last_sample;
app_fsm_t g_app_fsm;

void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    if (huart->Instance == USART2) {
        uint8_t b = s_rx_tmp;
        if (s_cj702_idx == 0 && b != CJ702_HEADER_0) {
            // wait for header
        } else if (s_cj702_idx == 1 && b != CJ702_HEADER_1) {
            s_cj702_idx = 0;
        } else {
            s_cj702_raw[s_cj702_idx++] = b;
            if (s_cj702_idx >= CJ702_FRAME_LEN) {
                cj702_data_t parsed;
                if (cj702_parse(s_cj702_raw, &parsed)) {
                    g_last_sample = parsed;
                    g_new_sample_ready = 1;
                }
                s_cj702_idx = 0;
            }
        }
        HAL_UART_Receive_IT(&huart2, &s_rx_tmp, 1);
    }
}
/* USER CODE END 1 */
```

注意：需确保 `huart2` 变量名与 CubeMX 生成的一致。

- [ ] **Step 5: 修改 `Core/Src/tim.c` 或 `stm32f1xx_it.c` 的 TIM2 回调**

在 `HAL_TIM_PeriodElapsedCallback` 中：

```c
/* USER CODE BEGIN Callback 0 */
#include "app_fsm.h"
extern app_fsm_t g_app_fsm;
/* USER CODE END Callback 0 */

/* ... */

/* USER CODE BEGIN Callback 1 */
if (htim->Instance == TIM2) {
    app_fsm_on_tick(&g_app_fsm);
}
/* USER CODE END Callback 1 */
```

- [ ] **Step 6: 提交**

```bash
git add test/stm32_cj702_lora_hal/Core/Inc/app_fsm.h test/stm32_cj702_lora_hal/Core/Src/app_fsm.c
git add test/stm32_cj702_lora_hal/Core/Src/main.c
git add test/stm32_cj702_lora_hal/Core/Src/usart.c
git add test/stm32_cj702_lora_hal/Core/Src/tim.c
git commit -m "feat(app_fsm): integrate sensor RX, aggregator and LoRa TX in main loop"
```

---

## Task 7: 编译与烧录

- [ ] **Step 1: 编译工程**

```bash
cd test/stm32_cj702_lora_hal
make
```

Expected: 生成 `build/stm32_cj702_lora_hal.elf`、`build/stm32_cj702_lora_hal.hex`、`build/stm32_cj702_lora_hal.bin`。

- [ ] **Step 2: 使用 ST-Link 烧录**

```bash
STM32_Programmer_CLI -c port=SWD -w build/stm32_cj702_lora_hal.hex -v -g
```

Expected: 烧录成功，程序运行。

- [ ] **Step 3: 提交（仅 Makefile 或构建脚本变更）**

```bash
git add -u
git commit -m "build: successful stm32 build and flash via ST-Link"
```

---

## Task 8: 硬件集成测试

- [ ] **Step 1: 接线检查**

| STM32 | CJ-702 | LoRa (E22) |
|---|---|---|
| 5V    | 5V     | 5V         |
| GND   | GND    | GND        |
| PA3   | TXD    | —          |
| PB10  | —      | RX         |
| PB11  | —      | TX         |

- [ ] **Step 2: 验证 USART2 数据接收**

使用逻辑分析仪或串口助手监听 PB11（USART3 TX），观察每 60 秒是否输出 20 字节自定义帧。

Expected：
- 帧头 `AA 55`
- 设备 ID `01`
- 消息类型 `01`
- payload 长度 `0E`
- 14 字节数据 + 1 字节 CRC8

- [ ] **Step 3: 验证异常处理**

断开 CJ-702 的 TXD，等待 60 秒。

Expected：USART3 输出 7 字节错误帧：`AA 55 01 FF 01 01 <CRC8>`。

- [ ] **Step 4: 验证 LED 状态**

- 正常运行：绿灯亮
- 发送瞬间：蓝灯闪烁
- 传感器异常：红灯亮

- [ ] **Step 5: 记录测试结果并提交日志**

在 `test/stm32_cj702_lora_hal/TESTS.md` 中记录测试步骤和结果。

```bash
git add test/stm32_cj702_lora_hal/TESTS.md
git commit -m "docs: record hardware integration test results"
```

---

## 自检清单

1. **Spec 覆盖检查**
   - USART2 接收 CJ-702：Task 1 + Task 6 ✓
   - 60 秒算术平均：Task 3 + Task 6 ✓
   - USART3 发送 LoRa：Task 1 + Task 5 + Task 6 ✓
   - 自定义二进制帧：Task 4 ✓
   - 错误处理：Task 6 ✓
   - RGB 状态灯：Task 5 + Task 6 ✓

2. **占位符扫描**
   - 无 TBD/TODO。
   - 每个任务含完整代码、命令、预期结果。

3. **类型一致性**
   - `cj702_data_t`、`aggregator_t`、`lora_frame_*` 接口在 spec 与 plan 中一致。
   - `g_app_fsm`、`g_aggregator`、`g_last_sample`、`g_new_sample_ready` 在 Task 6 中定义并在各模块间使用。
