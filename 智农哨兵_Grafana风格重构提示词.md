# 智农哨兵前端重构提示词（Grafana 工业暗色仪表盘风格）

## 项目背景
为农业机器人"智农哨兵"重构监控终端界面。当前功能包含：实时视频流、YOLO 作物检测、病害分类诊断、农艺建议、环境传感器（温度/湿度/CO2/土壤温湿度/叶面温度/光照/pH）、机器人方向控制、作物切换、急停按钮。参考 Grafana 工业监控仪表盘风格，打造专业级 IoT 暗色数据面板。

---

## 一、整体风格定位
- **风格**：Grafana / Industrial SCADA 暗色仪表盘
- **氛围**：硬核工业感、数据驱动、高信息密度、夜间/户外可读性强
- **背景**：极深蓝黑 `#0B1120`（主背景），卡片 `#111827` / `#1E293B`，避免纯黑 `#000000`
- **边框**：极细 1px 线条 `#1F2937`，hover 时边框高亮 `#374151`
- **圆角**：全局 4px（工业感直角微圆），按钮/药丸 6px
- **阴影**：无弥散阴影，仅用 1px 内发光/边框区分层级

---

## 二、色彩体系（严格遵循）
```
背景层级：
  页面底色:    #0B1120
  卡片底色:    #111827  (信息面板)
  次级卡片:    #1E293B  (图表容器、表格行hover)
  输入/控件:   #0F172A  (下拉框、按钮默认)

文字层级：
  主标题:      #F8FAFC  (18px, font-semibold)
  数据数值:    #FFFFFF  (28-36px, font-bold, 等宽字体)
  标签/单位:   #94A3B8  (12px, uppercase, tracking-wider)
  次要说明:    #64748B  (12px)

状态色（Grafana 标准）：
  正常/在线:   #10B981  (翡翠绿)
  警告/预警:   #F59E0B  (琥珀黄)
  危险/离线:   #EF4444  (红)
  信息/处理中:  #38BDF8  (天蓝)
  未知/等待:   #6B7280  (灰)

强调色：
  主强调:      #38BDF8  (天蓝，用于选中态、按钮激活、图表主色)
  次强调:      #A78BFA  (紫，用于次要图表线)
  视频边框:    #1E293B  常态，#38BDF8  选中/全屏
```

---

## 三、布局架构（12列网格）

### 顶部系统栏（高度 48px，固定）
- 左侧：Logo "智农哨兵" + 系统状态胶囊
  - AUTO（绿点呼吸）/ MANUAL（蓝点）/ 急停（红点闪烁）
- 中间：ROS 连接状态 + 网络延迟（ms）+ 当前时间（等宽字体，24h 格式）
- 右侧：报警计数器（红底白字徽章）+ 设置齿轮图标 + 全屏切换

### 主体区域（剩余高度，分三栏）

#### 左侧：视频与控制区（占 7/12 宽度）
- **视频面板**（高度占左侧 70%）：
  - 标题栏："实时画面 · 固定环境节点" + 录制红点（REC 闪烁）+ 分辨率标签
  - 视频区：深灰占位 `#0F172A`，中央加载动画（脉冲圆环 + "正在建立视频流..."）
  - 视频边框：1px solid `#1F2937`，视频在线时边框变为 `#10B981` 1px glow
  - 叠加 HUD：左上角显示 FPS、码率、丢包率（等宽字体，10px，半透明底）

- **控制面板**（高度占左侧 30%，紧接视频下方）：
  - 左侧：D-Pad 方向控制（上下左右 + 停止），圆形按钮 48px，默认 `#1E293B`，激活 `#38BDF8`，按下内阴影 inset
  - 中间：作物选择药丸组（番茄🍅 小麦🌾 草莓🍓），当前选中项 `#38BDF8` 背景白字，其余 `#1E293B` 灰字
  - 右侧：急停按钮（大红圆形，直径 64px，"急停"白字，hover 放大 1.05 + 阴影），状态条：IDLE | 检测:0 | 分析:0 | 进度: `[████░░░░░░] 0%`（等宽进度条）

#### 右侧：数据面板（占 5/12 宽度，垂直堆叠）
- **YOLO 植株检测卡片**（高度 20%）：
  - 标题："YOLO 植株检测" + 状态点（绿/黄/红）
  - 内容：检测数量大数字（"0" → 检测到后滚动动画 CountUp），下方置信度条
  - 底部：目标列表（作物类型 + 边界框坐标，等宽字体）

- **病害分类卡片**（高度 25%）：
  - 标题："病害分类 · TOMATO" + 作物下拉切换
  - 内容：饼图/环形图（Grafana 风格，细线，无填充），显示各病害占比
  - 列表：病害名 + 置信度百分比 + 颜色条（红/橙/绿）

- **农艺建议卡片**（高度 20%）：
  - 标题："农艺建议" + 建议数量徽章
  - 内容：滚动列表，每条建议带图标（💧灌溉/🌡升温/🐛虫害/✅正常），时间戳右对齐

- **预警趋势卡片**（高度 35%，最下方）：
  - 标题："预警趋势" + 时间范围选择（1h / 6h / 24h / 7d）
  - 内容：折线图（Grafana 风格），X轴时间，Y轴预警等级，多条线（温度/湿度/病害风险），网格线 `#1F2937`，tooltip 深色浮层
  - 底部：统计摘要（本周预警次数、最高风险等级、平均响应时间）

### 底部：环境传感器带（高度 80px，固定）
- 横向等分排列 8-9 个传感器卡片，每个卡片：
  - 顶部：图标 + 标签（12px，灰，uppercase）如 "TEMP" / "HUMI" / "CO2" / "SOIL-MOIST" / "SOIL-TEMP" / "LEAF-TEMP" / "LUX" / "PH"
  - 中间：数值（24px，等宽，白字），单位（12px，灰）
  - 底部：Sparkline 微型趋势图（过去30分钟，高 24px，无坐标轴，颜色随状态变：绿正常/黄警告/红危险）
  - 卡片背景：`#111827`，1px 边框 `#1F2937`，hover 边框 `#38BDF8`
  - 异常传感器：左侧边框 3px solid `#EF4444`，数值闪烁动画

---

## 四、字体规范
- **中文**：`Inter`, `"PingFang SC"`, `"Noto Sans SC"`, sans-serif
- **数据/英文/代码**：`"JetBrains Mono"`, `"Roboto Mono"`, monospace
- **数值**：tabular-nums（等宽数字，防止跳动）
- **标题**：14-18px，font-semibold，letter-spacing: 0.05em
- **数据**：24-36px，font-bold，line-height: 1
- **标签**：11-12px，font-medium，text-transform: uppercase，color: `#94A3B8`

---

## 五、交互与动效（克制，工业感）
- **数据刷新**：数值变化时，数字滚动（CountUp 效果），持续 300ms，ease-out
- **状态切换**：在线/离线过渡，边框颜色渐变 200ms，状态点呼吸动画（scale 1→1.2→1，2s infinite）
- **告警**：红色边框 + 背景脉冲（opacity 0.1→0.2，1s infinite），顶部系统栏报警计数器弹跳
- **视频加载**：中央双圆环旋转（外环 #38BDF8，内环 #1E293B），连接成功后圆环收缩消失
- **图表**：折线图绘制动画（从左到右 draw），tooltip 跟随鼠标，深色背景 `#0F172A` + 1px 边框
- **按钮**：按下时 translateY(1px) + inset shadow，急停按钮 hover 放大 + 红色 glow
- **卡片 hover**：边框颜色 `#374151`，无位移，无阴影扩散

---

## 六、组件细节规范

### 表格（病害列表/检测日志）
- 表头：背景 `#0F172A`，文字 `#94A3B8`，11px uppercase，底部 1px `#1F2937`
- 行高：36px，交替背景 `#111827` / `#0F172A`，hover `#1E293B`
- 状态列：左侧 4px 色条（绿/黄/红）+ 文字

### 图表（Grafana 风格）
- 网格线：1px dashed `#1F2937`
- 坐标轴文字：11px `#64748B`
- 折线：2px 宽，无填充或 10% 透明度填充
- 图例：顶部或右侧，药丸形，点击可隐藏线
- Tooltip：深色背景，圆角 4px，显示精确数值和时间

### 进度/条形
- 进度条：高度 4px，背景 `#1F2937`，填充 `#38BDF8`，圆角 2px
- 置信度条：高度 8px，分段色（红 0-30%，黄 30-70%，绿 70-100%）

---

## 七、技术栈建议
- **框架**：Vue3 + TypeScript（或 React）
- **样式**：Tailwind CSS（自定义上述颜色变量）
- **图表**：Apache ECharts（暗色主题，高度可定制，Grafana 风格容易实现）
- **字体**：Google Fonts / CDN 引入 JetBrains Mono + Inter
- **图标**：Lucide React/Vue（线条图标，工业感）
- **动画**：CSS transition + GSAP（复杂序列）或纯 CSS（简单状态）

---

## 八、输出要求
请生成一个完整的单文件 HTML（或 Vue/React 组件），包含：
1. 上述所有布局区域的骨架（视频区、右侧4张卡片、底部传感器带、顶部系统栏）
2. 完整的 CSS 变量定义（颜色、字体、间距）
3. 模拟数据填充（传感器数值、图表数据、病害列表）
4. 关键交互动效（状态呼吸灯、数字滚动、进度条、按钮按下效果）
5. 响应式适配：当屏幕宽度 < 1280px 时，右侧卡片折叠为底部标签页；< 768px 时垂直堆叠
6. 代码注释清晰，模块划分明确，方便后续接入真实 WebSocket/ROS 数据
