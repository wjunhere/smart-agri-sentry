##需求澄清

###一，开发范围

范围是全部RDK节点，但不必一下子全部生成完，按照你认为的优先级生成功能节点，列一个你能一下子看到的流程清单，以便于后续长期开发。我需要的是生产级完整实现

###二，ai推理模块

我已经训练好了一个模型，根据开源的plant_village数据集中的番茄叶片图片，相关文件已放到models文件夹中。模型输入尺寸是224×224。输出类别是番茄病害名称。TFLite最好采用RDK的NPU加速

### 三，硬件与运行环境

6.RDK上ros2版本是humble，

7.摄像头信号是mipi-csi，RDK官方摄像头，具体参数已写到docs/requirements/中的方案里，

8.GPS模块NMEA 输出频率实际是1Hz。

9.STM32端的固件同步开发中，协议帧的格式并没有完全确定。

###四，web服务端

10.web页面需要数据展示（传感器读数、GPS 位置、AI 诊断结果），还需要远程控制（遥控方向、模式切换、云台角度、航点设置）

11.前端技术采用vue，先做个轻量化的demo，**ROS2 + rosbridge_server + FastAPI 网关**，数据流标准化

12.websocket推送频率按数据类型分层

| 数据类型                                  | 推送频率                                   | 场景说明                                                     |
| ----------------------------------------- | ------------------------------------------ | ------------------------------------------------------------ |
| **传感器环境数据**（土壤/空气/CO₂）       | **1 Hz（巡检模式）/ 5 Hz（精细监测模式）** | 自主巡逻时 1 秒一次足够；人工介入采样时提高到 5 Hz           |
| **AI 诊断结果**（病害风险、置信度）       | **2 Hz（与推理任务对齐）**                 | 每 500ms 推理一次，结果有变化才推送，或固定 2 Hz 心跳        |
| **底盘状态**（电量、速度、GPS坐标、模式） | **2 Hz**                                   | 0.5m/s 速度下，2 Hz 意味着每 25cm 更新一次位置，足够绘制轨迹 |
| **紧急报警**（病害高风险、低电量、碰撞）  | **事件触发（立即）+ 1 Hz 确认**            | 不等待周期，检测到异常立即推，随后 1 Hz 重复直到解除         |
| **远程控制指令**（人工接管时）            | **20-50 Hz**                               | 与 STM32 控制算法任务的 20ms（50Hz）对齐，保证操控跟手       |
| **视频流**                                | **独立 HTTP MJPEG 流，15-20 fps**          | **不走 WebSocket**，用单独的 `/video` 端点，避免二进制帧污染 WS 通道 |

###五，导航与融合逻辑

13.gps航点采用改良版纯追踪 + 航点分层管理，区分"路径点"与"任务点"，**路径点**：纯追踪过垄，允许轻微超调，不要求严格航向，**任务点**：到达后先原地转向对准作物行，再执行机械臂/摄像头动作。

14.航点采用**YAML 存"模板路线"，SQLite 管"当前执行"，Web 界面做"可视化编辑"**的综合路线

15. fusion_node的融合权重策略还不太确定，目前打算 采用 **「条件门控 + 自适应权重」** 的融合策略，先根据环境条件判断**当前场景属于哪种诊断模式**，不同模式权重不一样，权重的确定初期用专家 AHP 定权，同时采集 200-500 组"图像+传感器+人工标注"样本，用**逻辑回归**或**轻量神经网络**训练权重

###六，工程化与部署

16.需要生成ros2包完整结构

17.代码质量能跑就行

### 七，额外补充

导航的功能可以暂时不用做，缺失太多已知信息的功能可以不做，先做份demo出来，一定要列一份清单，标注需要完成的功能，做完的标记完成，放到你可以看到的地方，以便后续的开发。另外，我补充的信息可以修改到project_context中