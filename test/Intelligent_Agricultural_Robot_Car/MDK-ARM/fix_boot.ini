/* Keil Debug init script — bypass BOOT0 and run from flash */

FUNC void FixBootAndRun(void) {
    /* Force PC to flash start — ignores whatever BOOT0 selected */
    PC = 0x08000000;
    printf("PC forced to 0x08000000, resuming...\n");
}

/* Execute on debug session start */
FixBootAndRun();
